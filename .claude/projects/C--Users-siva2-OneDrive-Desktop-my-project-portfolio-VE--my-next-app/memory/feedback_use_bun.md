---
name: Use bun for builds
description: User prefers bun over npm/npx for running builds and scripts
type: feedback
---

Use `bun run` instead of `npm run` or `npx` for build commands and scripts.

**Why:** User explicitly corrected when npx was used for build.
**How to apply:** Always use `bun run build`, `bun run dev`, etc. for this project.
